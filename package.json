{
  "name": "dot-prompts-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w dot-prompts && npm run build -w @dot-prompts/pi && npm run build -w @dot-prompts/conformance",
    "build:core": "npm run build -w dot-prompts",
    "build:pi": "npm run build -w dot-prompts && npm run build -w @dot-prompts/pi",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "npm run dev -w dot-prompts"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.8.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=18"
  }
}
